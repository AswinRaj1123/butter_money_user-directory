# User Directory App

A simple React app for managing users, built for Butter Money assessment.

## Setup
- Clone the repo: `git clone https://github.com/AswinRaj1123/butter_money_user-directory.git`
- Install dependencies: `npm install`
- Run locally: `npm start`

## Features
(Will update as I implement)

## Assumptions
(Will add if any)